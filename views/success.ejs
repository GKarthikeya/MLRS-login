<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Success Page</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"/>
  <style>
    /* Same styling as before */
  </style>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-8 success-container">
        <h1 class="success-message">✅ Form Submitted Successfully!</h1>

        <div class="submitted-info">
          <p><strong>👤 Username:</strong> <span id="userDisplay"></span></p>
          <p><strong>🔑 Password:</strong> <span id="passDisplay"></span></p>
        </div>

        <a href="login.html" class="custom-button">Back to Login</a>
      </div>
    </div>
  </div>

  <script>
    // Get values from sessionStorage
    const username = sessionStorage.getItem("username");
    const password = sessionStorage.getItem("password");

    // Display on the page
    document.getElementById("userDisplay").textContent = username || "N/A";
    document.getElementById("passDisplay").textContent = password || "N/A";
  </script>
</body>
</html>
